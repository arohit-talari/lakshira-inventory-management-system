# Pin versions before every release to avoid surprise breakage from upstream updates
# Runtime dependencies for inventory.py / generate_report.py / scheduler.py
gspread==6.2.1
oauth2client==4.1.3
phonenumbers==9.0.35
questionary==2.1.1
reportlab==5.0.0
anthropic==0.120.2
prompt_toolkit
python-dotenv
requests

# Test-only dependencies (Tier 1 + Tier 2, see tests/)
pytest
pexpect
pytest-rerunfailures
